import { useState, type ReactNode } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';
import { Route, Switch, useLocation, Router as WouterRouter } from 'wouter';
import {
  ArrowLeft, ArrowRight, BarChart3, BookOpen, BrainCircuit, BriefcaseBusiness, Check, ChevronDown,
  CircleHelp, CircleUserRound, ClipboardCheck, Code2, Compass, FileText, Github, GraduationCap,
  Headphones, LayoutDashboard, Lightbulb, ListChecks, Menu, Mic2, MoreHorizontal, Network, Play,
  Plus, Radar, RefreshCw, Rocket, Search, Settings, Sparkles, Target, Trophy, UserRound, X, Zap
} from 'lucide-react';

const queryClient = new QueryClient();

type ButtonProps = { children: ReactNode; onClick?: () => void; variant?: 'primary' | 'ghost' | 'outline'; className?: string; disabled?: boolean; testId?: string };
function Button({ children, onClick, variant = 'primary', className = '', disabled, testId }: ButtonProps) {
  const base = 'inline-flex items-center justify-center gap-2 rounded-lg px-4 py-2.5 text-sm font-semibold transition-all duration-200 disabled:cursor-not-allowed disabled:opacity-40';
  const variants = {
    primary: 'bg-cyan-300 text-slate-950 hover:bg-cyan-200 shadow-[0_0_22px_rgba(87,215,245,.15)]',
    ghost: 'text-slate-300 hover:bg-white/[.06] hover:text-white',
    outline: 'border border-slate-700/80 bg-slate-900/40 text-slate-200 hover:border-cyan-300/50 hover:bg-cyan-300/[.06]'
  };
  return <button data-testid={testId} onClick={onClick} disabled={disabled} className={`${base} ${variants[variant]} ${className}`}>{children}</button>;
}

function Brand({ compact = false }: { compact?: boolean }) {
  return <div className="flex items-center gap-3" data-testid="brand-mark">
    <div className="relative grid size-8 place-items-center rounded-full border border-cyan-200/50 bg-cyan-200/10">
      <div className="size-2 rounded-full bg-cyan-200 shadow-[0_0_13px_5px_rgba(111,226,249,.45)]" />
    </div>
    {!compact && <div><div className="display text-[15px] font-semibold tracking-tight text-white">SkillGap <span className="text-cyan-200">AI</span></div><div className="mono text-[8px] uppercase tracking-[.24em] text-slate-500">opportunity readiness</div></div>}
  </div>;
}

const journey = [
  ['01', 'Analyze', 'See what the opportunity actually asks for.', Radar],
  ['02', 'Find gaps', 'Turn a vague worry into a focused map.', Target],
  ['03', 'Learn', 'Build a sequence that respects your time.', BookOpen],
  ['04', 'Practice', 'Train on the decisions that matter.', Code2],
  ['05', 'Prove', 'Collect evidence, not just course badges.', Trophy],
  ['06', 'Get ready', 'Walk in with a story you can stand behind.', Rocket],
] as const;

function Landing({ enter }: { enter: () => void }) {
  const [showMore, setShowMore] = useState(false);
  return <div className="noise portal-bg min-h-[100dvh] overflow-hidden">
    <header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-6 lg:px-10" data-testid="landing-header">
      <Brand />
      <nav className="hidden items-center gap-7 text-sm text-slate-400 md:flex">
        <a href="#method" className="hover:text-white">How it works</a>
        <a href="#proof" className="hover:text-white">Proof loop</a>
        <a href="#resources" className="hover:text-white">Resources</a>
      </nav>
      <div className="flex items-center gap-2"><Button variant="ghost" className="hidden sm:inline-flex" onClick={enter} testId="button-sign-in">Sign in</Button><Button onClick={enter} testId="button-start">Start your map <ArrowRight size={15} /></Button></div>
    </header>

    <main>
      <section className="relative mx-auto grid min-h-[680px] max-w-7xl items-center gap-14 px-5 pb-20 pt-16 lg:grid-cols-[1.05fr_.95fr] lg:px-10 lg:pb-28 lg:pt-20">
        <div className="relative z-10 enter">
          <div className="mb-7 inline-flex items-center gap-2 rounded-full border border-cyan-200/20 bg-cyan-100/[.06] px-3 py-1.5 text-[11px] font-semibold uppercase tracking-[.2em] text-cyan-100"><span className="size-1.5 rounded-full bg-cyan-200 shadow-[0_0_10px_3px_rgba(118,225,250,.55)]" />A clearer way forward</div>
          <h1 className="display max-w-2xl text-5xl font-semibold leading-[.98] tracking-[-.05em] text-white sm:text-7xl">The distance between <span className="text-gradient">where you are</span> and what comes next.</h1>
          <p className="mt-7 max-w-xl text-lg leading-8 text-slate-400">SkillGap AI turns an intimidating opportunity into a grounded plan. Understand what matters. Build the proof. Show up ready.</p>
          <div className="mt-9 flex flex-wrap gap-3"><Button onClick={enter} className="px-5 py-3.5" testId="button-hero-cta">Enter the portal <ArrowRight size={16} /></Button><Button variant="outline" onClick={() => document.getElementById('method')?.scrollIntoView({ behavior: 'smooth' })} testId="button-see-method">See the method <ChevronDown size={16} /></Button></div>
          <div className="mt-10 flex items-center gap-5 text-xs text-slate-500"><div className="flex -space-x-2">{['AM', 'JR', 'SK'].map((x) => <div key={x} className="grid size-7 place-items-center rounded-full border-2 border-[#090b15] bg-gradient-to-br from-slate-500 to-indigo-400 text-[9px] font-bold text-white">{x}</div>)}</div><span>Built for the moment before you feel ready.</span></div>
        </div>
        <div className="relative flex min-h-[450px] items-center justify-center">
          <div className="absolute size-[390px] rounded-full border border-cyan-100/10 sm:size-[500px]" />
          <div className="absolute size-[285px] rounded-full border border-dashed border-purple-200/15 sm:size-[370px]" />
          <div className="absolute size-[210px] rounded-full border border-cyan-200/20 sm:size-[270px]" />
          <div className="orb animate-float relative grid size-44 place-items-center rounded-full bg-[radial-gradient(circle_at_35%_28%,#d9fbff_0,#69d7ec_12%,#26558e_42%,#191536_73%,#080a13_100%)] sm:size-56">
            <div className="size-20 rounded-full bg-[#d9fbff]/30 blur-2xl" />
            <div className="absolute -top-7 left-1/2 h-14 w-px bg-gradient-to-b from-transparent to-cyan-200/50" />
          </div>
          <div className="glass absolute left-1 top-9 hidden w-44 rounded-xl p-3 sm:block"><div className="mono text-[9px] uppercase tracking-widest text-cyan-200">target opportunity</div><div className="mt-2 text-sm font-semibold text-white">Product Designer</div><div className="mt-1 text-xs text-slate-500">your next north star</div></div>
          <div className="glass absolute bottom-12 right-0 hidden w-48 rounded-xl p-3 sm:block"><div className="flex items-center justify-between"><span className="mono text-[9px] uppercase tracking-widest text-slate-500">readiness</span><span className="text-xs text-emerald-300">+12%</span></div><div className="mt-2 flex items-end gap-1"><span className="display text-2xl font-semibold text-white">68</span><span className="pb-1 text-xs text-slate-500">/ 100</span></div><div className="mt-2 h-1.5 rounded-full bg-slate-800"><div className="h-full w-[68%] rounded-full bg-gradient-to-r from-cyan-300 to-purple-300" /></div></div>
          <div className="absolute bottom-1 left-1/2 flex -translate-x-1/2 items-center gap-2 text-[10px] uppercase tracking-[.3em] text-slate-600"><span className="size-1 rounded-full bg-cyan-300" />your clarity layer</div>
        </div>
      </section>

      <section id="method" className="border-y border-white/[.06] bg-[#0c0f1d]/60 px-5 py-20 lg:px-10 lg:py-28">
        <div className="mx-auto max-w-7xl"><div className="max-w-xl"><div className="mono text-[10px] uppercase tracking-[.25em] text-cyan-200">the transformation</div><h2 className="display mt-4 text-4xl font-semibold tracking-[-.04em] text-white sm:text-5xl">From noise to a next move.</h2><p className="mt-4 text-slate-400">No more collecting tabs and hoping they add up. Each step changes what you can do next.</p></div>
          <div className="mt-14 grid gap-px overflow-hidden rounded-2xl border border-white/[.08] bg-white/[.07] md:grid-cols-3">
            {journey.map(([num, title, desc, Icon], i) => <div key={title} className="group relative bg-[#0d1120] p-6 transition-colors hover:bg-[#111a2c]"><div className="flex items-center justify-between"><span className="mono text-xs text-slate-600">{num}</span><Icon size={18} strokeWidth={1.5} className="text-cyan-200/70" /></div><h3 className="display mt-12 text-xl font-semibold text-white">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{desc}</p>{i < 5 && <div className="absolute -right-2 top-1/2 z-10 hidden size-4 rotate-45 border-r border-t border-white/10 bg-[#0d1120] md:block" />}</div>)}
          </div>
        </div>
      </section>

      <section id="proof" className="mx-auto grid max-w-7xl items-center gap-16 px-5 py-24 lg:grid-cols-[.9fr_1.1fr] lg:px-10">
        <div><div className="mono text-[10px] uppercase tracking-[.25em] text-purple-200">why it works</div><h2 className="display mt-4 text-4xl font-semibold tracking-[-.04em] text-white sm:text-5xl">A readiness system, not another score.</h2><p className="mt-5 max-w-md leading-7 text-slate-400">The strongest signal is not what you say you know. It is what you can explain, make, and point to.</p><Button onClick={enter} className="mt-8" testId="button-build-plan">Build my readiness map <ArrowRight size={15} /></Button></div>
        <div className="relative grid gap-3 sm:grid-cols-2"><div className="glass rounded-2xl p-5 sm:translate-y-8"><BrainCircuit className="text-cyan-200" size={21}/><h3 className="mt-12 font-semibold text-white">Context over keywords</h3><p className="mt-2 text-sm leading-6 text-slate-500">See why a skill matters in this role, not just whether it appeared in a list.</p></div><div className="glass rounded-2xl p-5"><ClipboardCheck className="text-purple-200" size={21}/><h3 className="mt-12 font-semibold text-white">Evidence over intention</h3><p className="mt-2 text-sm leading-6 text-slate-500">Convert small projects and practice into a body of proof you own.</p></div><div className="glass rounded-2xl p-5"><BarChart3 className="text-emerald-200" size={21}/><h3 className="mt-12 font-semibold text-white">Progress you can feel</h3><p className="mt-2 text-sm leading-6 text-slate-500">A living view of momentum, with the next useful action always in reach.</p></div><div className="rounded-2xl border border-cyan-200/15 bg-gradient-to-br from-cyan-200/[.08] to-purple-200/[.04] p-5 sm:translate-y-8"><div className="mono text-[10px] uppercase tracking-widest text-cyan-200">your proof loop</div><div className="mt-5 flex flex-wrap gap-2">{['learn','make','reflect','show'].map((x, i) => <div key={x} className="flex items-center gap-2 text-xs text-slate-300"><span className="grid size-6 place-items-center rounded-full border border-cyan-200/30 text-[10px] text-cyan-200">{i + 1}</span>{x}</div>)}</div><div className="mt-7 h-px bg-gradient-to-r from-cyan-300/50 via-purple-300/30 to-transparent" /><p className="mt-4 text-sm text-slate-400">Small, connected wins compound.</p></div></div>
      </section>

      <section id="resources" className="border-t border-white/[.06] px-5 py-20 lg:px-10"><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-5"><div><div className="mono text-[10px] uppercase tracking-[.25em] text-cyan-200">inside the portal</div><h2 className="display mt-4 text-3xl font-semibold text-white">Everything has a place.</h2></div><button onClick={() => setShowMore(!showMore)} className="flex items-center gap-2 text-sm text-cyan-200 hover:text-cyan-100" data-testid="button-resources-toggle">{showMore ? 'Show less' : 'Explore the toolkit'} <ArrowRight size={14}/></button></div><div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{(showMore ? ['Opportunity tracker', 'Skill gap map', 'Practice studio', 'Evidence vault', 'Learning paths', 'GitHub signal', 'Resume lens', 'Interview room'] : ['Opportunity tracker', 'Skill gap map', 'Practice studio', 'Evidence vault']).map((x, i) => <div key={x} className="glass rounded-xl p-5"><div className="mono text-[10px] text-slate-600">0{i + 1}</div><h3 className="mt-8 font-semibold text-white">{x}</h3><p className="mt-2 text-xs leading-5 text-slate-500">{['Compare roles without losing the thread.', 'Know which gap deserves your energy.', 'Practice the work, not abstract quizzes.', 'Keep your strongest signals close.'][i % 4]}</p></div>)}</div></div></section>
      <section className="mx-5 mb-20 overflow-hidden rounded-3xl border border-cyan-200/15 bg-[radial-gradient(circle_at_80%_50%,rgba(74,145,217,.18),transparent_37%),#0d1222] px-6 py-16 text-center lg:mx-auto lg:max-w-7xl lg:px-10"><div className="mx-auto max-w-2xl"><div className="mx-auto grid size-10 place-items-center rounded-full border border-cyan-200/30 bg-cyan-200/10"><Sparkles className="text-cyan-200" size={17}/></div><h2 className="display mt-6 text-4xl font-semibold tracking-[-.04em] text-white">Ready is not a feeling.<br/><span className="text-cyan-200">It is a trail you can follow.</span></h2><p className="mt-4 text-slate-400">Start with one opportunity. Leave with a map.</p><Button onClick={enter} className="mt-8" testId="button-final-cta">Open my portal <ArrowRight size={15}/></Button></div></section>
    </main>
    <footer className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-4 border-t border-white/[.06] px-5 py-7 text-xs text-slate-600 lg:px-10"><Brand compact/><span>Demo experience · Built for thoughtful progress</span></footer>
  </div>;
}

const onboardingSteps = [
  { key: 'account', label: 'About you', icon: UserRound, title: 'Let’s make this yours.', desc: 'A little context helps us keep the map grounded.', fields: [['name', 'What should we call you?', 'Maya Chen'], ['email', 'Email address', 'maya.chen@example.com']] },
  { key: 'education', label: 'Education', icon: GraduationCap, title: 'Where are you starting from?', desc: 'Your path is not a prerequisite. It is useful context.', fields: [['school', 'School or program', 'University of Washington'], ['study', 'What are you studying?', 'Human-centered design']] },
  { key: 'goals', label: 'Direction', icon: Compass, title: 'What kind of work pulls you in?', desc: 'Choose a direction. You can change it when your curiosity moves.', fields: [['goal', 'Target role', 'Product Designer'], ['time', 'Weekly time to invest', '5–7 hours']] },
  { key: 'opportunity', label: 'Opportunity', icon: BriefcaseBusiness, title: 'Give your next opportunity a shape.', desc: 'Use a real target or start with a role you want to explore.', fields: [['role', 'Opportunity title', 'Associate Product Designer'], ['company', 'Company or program', 'Northstar Labs']] },
  { key: 'skills', label: 'Skills', icon: Zap, title: 'What can you already do?', desc: 'No judgment here. Honest signals create a better route.', fields: [['skills', 'Skills you feel good about', 'Figma, user interviews, prototyping'], ['growth', 'Skills you want to grow', 'Product strategy, analytics']] },
  { key: 'projects', label: 'Proof', icon: Code2, title: 'What have you made?', desc: 'Small work counts. We are looking for moments you can talk about.', fields: [['project', 'Project or class work', 'Transit app redesign'], ['detail', 'What did you own?', 'Research, flows, usability test']] },
  { key: 'signals', label: 'Signals', icon: Github, title: 'Bring the signals you have.', desc: 'Demo-only analysis. Nothing is connected or uploaded here.', fields: [['github', 'GitHub profile (optional)', 'github.com/mayachen'], ['resume', 'Resume status', 'I have a resume to review']] },
  { key: 'rhythm', label: 'Rhythm', icon: Headphones, title: 'How do you learn best?', desc: 'We will shape your first week around your actual energy.', fields: [['style', 'Preferred learning mode', 'A mix of examples and practice'], ['focus', 'Best focus window', 'Evenings, 6–9pm']] }
];

function Onboarding({ finish, back }: { finish: () => void; back: () => void }) {
  const [step, setStep] = useState(0);
  const [values, setValues] = useState<Record<string, string>>({});
  const current = onboardingSteps[step];
  const Icon = current.icon;
  const update = (k: string, v: string) => setValues((prev) => ({ ...prev, [k]: v }));
  return <div className="noise portal-bg min-h-[100dvh]"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-6 lg:px-8"><button onClick={back} data-testid="button-onboarding-back"><Brand/></button><div className="mono text-[10px] uppercase tracking-[.22em] text-slate-500">your readiness map <span className="ml-2 text-cyan-200">{String(step + 1).padStart(2, '0')} / 08</span></div><button onClick={finish} className="text-xs text-slate-500 hover:text-white" data-testid="button-skip-onboarding">Skip for now</button></header><div className="mx-auto max-w-6xl px-5 pb-12 lg:px-8"><div className="mb-12 flex gap-1">{onboardingSteps.map((s, i) => <button key={s.key} onClick={() => setStep(i)} aria-label={`Go to ${s.label}`} className={`h-1 flex-1 rounded-full transition-colors ${i <= step ? 'bg-cyan-300' : 'bg-slate-800'}`} data-testid={`step-${s.key}`} />)}</div><div className="mx-auto max-w-2xl enter"><div className="mb-8 flex size-12 items-center justify-center rounded-2xl border border-cyan-200/20 bg-cyan-200/[.07]"><Icon className="text-cyan-200" size={22}/></div><div className="mono text-[10px] uppercase tracking-[.25em] text-cyan-200">step {String(step + 1).padStart(2, '0')} · {current.label}</div><h1 className="display mt-4 text-4xl font-semibold tracking-[-.04em] text-white sm:text-5xl">{current.title}</h1><p className="mt-4 max-w-lg text-slate-400">{current.desc}</p><div className="glass mt-10 space-y-6 rounded-2xl p-6 sm:p-8">{current.fields.map(([key, label, placeholder]) => <label key={key} className="block"><span className="mb-2 block text-sm font-medium text-slate-300">{label}</span><input data-testid={`input-${key}`} value={values[key] ?? ''} onChange={(e) => update(key, e.target.value)} placeholder={placeholder} className="w-full rounded-lg border border-slate-700/80 bg-slate-950/40 px-4 py-3 text-sm text-white outline-none transition-colors placeholder:text-slate-600 focus:border-cyan-200/60 focus:ring-2 focus:ring-cyan-300/10"/></label>)}</div><div className="mt-7 flex items-center justify-between"><Button variant="ghost" disabled={step === 0} onClick={() => setStep(Math.max(0, step - 1))} testId="button-previous-step"><ArrowLeft size={15}/> Back</Button><Button onClick={() => step === onboardingSteps.length - 1 ? finish() : setStep(step + 1)} testId="button-next-step">{step === onboardingSteps.length - 1 ? 'See my map' : 'Continue'} <ArrowRight size={15}/></Button></div></div></div></div>;
}

type PageKey = 'overview' | 'gaps' | 'learn' | 'practice' | 'evidence' | 'opportunities' | 'journey' | 'reference' | 'settings';
const navGroups = [
  { title: 'Your map', items: [['overview', 'Overview', LayoutDashboard], ['gaps', 'Skill gaps', Target], ['learn', 'Learning plan', BookOpen], ['practice', 'Practice studio', Code2], ['evidence', 'Evidence vault', Trophy]] as const },
  { title: 'Explore', items: [['opportunities', 'Opportunities', BriefcaseBusiness], ['journey', 'Progress journey', Network], ['reference', 'Quick reference', ListChecks]] as const },
];

function AppShell({ exit }: { exit: () => void }) {
  const [page, setPage] = useState<PageKey>('overview');
  const [mobile, setMobile] = useState(false);
  const [checked, setChecked] = useState<string[]>(['research']);
  const [toast, setToast] = useState('');
  const select = (p: PageKey) => { setPage(p); setMobile(false); };
  const notify = (x: string) => { setToast(x); window.setTimeout(() => setToast(''), 2500); };
  const pageTitle: Record<PageKey, string> = { overview: 'Overview', gaps: 'Skill gaps', learn: 'Learning plan', practice: 'Practice studio', evidence: 'Evidence vault', opportunities: 'Opportunities', journey: 'Progress journey', reference: 'Quick reference', settings: 'Settings' };
  return <div className="noise min-h-[100dvh] bg-[#080a13] text-slate-200"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-white/[.07] bg-[#0a0d18] px-4 py-6 transition-transform lg:translate-x-0 ${mobile ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center justify-between px-2"><Brand/><button className="lg:hidden" onClick={() => setMobile(false)} data-testid="button-close-menu"><X size={18}/></button></div><div className="mt-10 space-y-7">{navGroups.map((group) => <div key={group.title}><div className="mono mb-2 px-3 text-[9px] uppercase tracking-[.23em] text-slate-600">{group.title}</div><div className="space-y-1">{group.items.map(([key, label, Icon]) => <button key={key} onClick={() => select(key)} data-testid={`nav-${key}`} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-left text-sm transition-colors ${page === key ? 'bg-cyan-200/[.09] text-cyan-100' : 'text-slate-400 hover:bg-white/[.04] hover:text-white'}`}><Icon size={16} strokeWidth={1.7}/>{label}{key === 'gaps' && <span className="ml-auto rounded-full bg-purple-300/15 px-1.5 py-0.5 text-[10px] text-purple-200">4</span>}</button>)}</div></div>)}</div><div className="absolute bottom-5 left-4 right-4 space-y-1 border-t border-white/[.07] pt-4"><button onClick={() => select('settings')} className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-slate-400 hover:bg-white/[.04] hover:text-white" data-testid="nav-settings"><Settings size={16}/> Settings</button><button onClick={exit} className="flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-slate-500 hover:text-white" data-testid="button-exit-app"><ArrowLeft size={16}/> Return to landing</button></div></aside><div className="lg:pl-64"><header className="sticky top-0 z-30 flex h-[72px] items-center justify-between border-b border-white/[.07] bg-[#080a13]/80 px-5 backdrop-blur-xl lg:px-9"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={() => setMobile(true)} data-testid="button-open-menu"><Menu size={21}/></button><div className="hidden text-sm text-slate-500 sm:block">My readiness map <span className="mx-2 text-slate-700">/</span> <span className="text-slate-200">{pageTitle[page]}</span></div><div className="sm:hidden"><Brand compact/></div></div><div className="flex items-center gap-3"><button className="hidden items-center gap-2 rounded-lg border border-slate-800 px-3 py-2 text-xs text-slate-400 hover:text-white sm:flex" onClick={() => notify('Demo search is ready for your next question.')} data-testid="button-search"><Search size={14}/> Search <span className="mono ml-2 text-[9px] text-slate-600">⌘ K</span></button><button className="relative rounded-full border border-slate-700 bg-slate-900 p-2 text-cyan-200" onClick={() => notify('You are all caught up.')} data-testid="button-notifications"><CircleHelp size={16}/><span className="absolute -right-0.5 -top-0.5 size-2 rounded-full bg-cyan-300"/></button><div className="grid size-8 place-items-center rounded-full border border-purple-200/30 bg-purple-200/10 text-xs font-semibold text-purple-100" data-testid="avatar-user">MC</div></div></header><main className="mx-auto max-w-7xl px-5 py-8 lg:px-9 lg:py-10">{page === 'overview' && <Overview go={select} notify={notify}/>} {page === 'gaps' && <Gaps go={select}/>} {page === 'learn' && <Learn notify={notify}/>} {page === 'practice' && <Practice checked={checked} setChecked={setChecked} notify={notify}/>} {page === 'evidence' && <Evidence notify={notify}/>} {page === 'opportunities' && <Opportunities notify={notify}/>} {page === 'journey' && <Journey/>} {page === 'reference' && <Reference/>} {page === 'settings' && <SettingsPanel notify={notify}/>}</main></div>{toast && <div className="fixed bottom-6 right-6 z-50 flex items-center gap-2 rounded-lg border border-cyan-200/25 bg-[#111a2b] px-4 py-3 text-sm text-cyan-100 shadow-xl" data-testid="status-toast"><Check size={15}/>{toast}</div>}</div>;
}

function SectionHead({ eyebrow, title, desc, action }: { eyebrow: string; title: string; desc?: string; action?: ReactNode }) { return <div className="flex flex-wrap items-end justify-between gap-5"><div><div className="mono text-[10px] uppercase tracking-[.23em] text-cyan-200">{eyebrow}</div><h1 className="display mt-3 text-3xl font-semibold tracking-[-.04em] text-white sm:text-4xl">{title}</h1>{desc && <p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">{desc}</p>}</div>{action}</div>; }
function Stat({ label, value, detail, accent = 'cyan' }: { label: string; value: string; detail: string; accent?: 'cyan' | 'purple' | 'green' }) { return <div className="glass rounded-xl p-5"><div className="flex items-center justify-between"><span className="mono text-[9px] uppercase tracking-widest text-slate-500">{label}</span><span className={`size-2 rounded-full ${accent === 'purple' ? 'bg-purple-300' : accent === 'green' ? 'bg-emerald-300' : 'bg-cyan-300'}`}/></div><div className="display mt-5 text-3xl font-semibold text-white">{value}</div><div className="mt-1 text-xs text-slate-500">{detail}</div></div>; }
function Overview({ go, notify }: { go: (p: PageKey) => void; notify: (s: string) => void }) { return <div className="enter space-y-9"><SectionHead eyebrow="Tuesday, October 8" title="Good evening, Maya." desc="Your path to Associate Product Designer is taking shape." action={<Button variant="outline" onClick={() => notify('Demo profile saved.')} testId="button-edit-profile"><CircleUserRound size={15}/> Edit profile</Button>}/><div className="grid gap-4 md:grid-cols-3"><Stat label="Readiness" value="68 / 100" detail="↑ 12 points this month" /><Stat label="Focus this week" value="2h 40m" detail="of your 5–7 hour rhythm" accent="purple"/><Stat label="Proof collected" value="04 pieces" detail="1 ready to share" accent="green"/></div><div className="grid gap-5 xl:grid-cols-[1.25fr_.75fr]"><div className="glass rounded-2xl p-6 sm:p-7"><div className="flex items-start justify-between"><div><div className="mono text-[10px] uppercase tracking-[.2em] text-slate-500">target opportunity</div><h2 className="display mt-2 text-2xl font-semibold text-white">Associate Product Designer</h2><p className="mt-1 text-sm text-slate-500">Northstar Labs · Seattle / Remote</p></div><span className="rounded-full border border-cyan-200/20 bg-cyan-200/[.07] px-2.5 py-1 text-[10px] text-cyan-100">active map</span></div><div className="mt-8 grid gap-8 sm:grid-cols-[150px_1fr] sm:items-center"><div className="relative mx-auto grid size-36 place-items-center rounded-full" style={{ background: 'conic-gradient(#75e4f4 68%, #232941 0)' }}><div className="grid size-28 place-items-center rounded-full bg-[#101527]"><div className="text-center"><div className="display text-3xl font-semibold text-white">68</div><div className="mono text-[9px] uppercase tracking-widest text-slate-600">ready</div></div></div></div><div className="space-y-4">{[['Foundations', '82%', 'bg-cyan-300'], ['Craft & tools', '64%', 'bg-purple-300'], ['Proof & story', '52%', 'bg-emerald-300']].map(([x, n, c]) => <div key={x}><div className="mb-1.5 flex justify-between text-xs"><span className="text-slate-400">{x}</span><span className="text-slate-300">{n}</span></div><div className="h-1.5 rounded-full bg-slate-800"><div className={`h-full rounded-full ${c}`} style={{ width: n }} /></div></div>)}</div></div><button onClick={() => go('gaps')} className="mt-8 flex items-center gap-2 text-sm font-semibold text-cyan-200 hover:text-white" data-testid="button-view-gaps">See what moves the needle <ArrowRight size={15}/></button></div><div className="rounded-2xl border border-purple-200/15 bg-[radial-gradient(circle_at_65%_20%,rgba(126,98,221,.22),transparent_43%),#111225] p-6"><div className="flex items-center justify-between"><div className="mono text-[10px] uppercase tracking-[.2em] text-purple-200">next best action</div><Sparkles className="text-purple-200" size={17}/></div><h3 className="display mt-12 text-2xl font-semibold text-white">Practice a trade-off.</h3><p className="mt-3 text-sm leading-6 text-slate-400">A 12-minute scenario on prioritizing a feature when the evidence is incomplete.</p><Button onClick={() => go('practice')} className="mt-7 w-full" testId="button-start-practice">Start practice <Play size={14}/></Button><div className="mt-4 text-center text-[11px] text-slate-600">No grades. Just a stronger point of view.</div></div></div><div className="grid gap-5 md:grid-cols-2"><div className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><h3 className="font-semibold text-white">This week’s rhythm</h3><button onClick={() => go('learn')} className="text-xs text-cyan-200" data-testid="button-open-plan">Open plan</button></div><div className="mt-6 space-y-4">{([['Mon', 'Read: product sense foundations', true], ['Wed', 'Practice: prioritize the backlog', false], ['Sat', 'Make: update your case study', false]] as Array<[string, string, boolean]>).map(([day, task, done]) => <div className="flex gap-3" key={day}><div className={`mt-0.5 grid size-5 place-items-center rounded-full border ${done ? 'border-cyan-200 bg-cyan-200 text-slate-950' : 'border-slate-700 text-transparent'}`}><Check size={12}/></div><div><div className="text-xs text-slate-600">{day}</div><div className={`text-sm ${done ? 'text-slate-500 line-through' : 'text-slate-300'}`}>{task}</div></div></div>)}</div></div><div className="glass rounded-2xl p-6"><h3 className="font-semibold text-white">Your signal stack</h3><p className="mt-2 text-xs text-slate-500">A quick snapshot of what is making your story credible.</p><div className="mt-6 flex flex-wrap gap-2">{['Figma', 'User research', 'Prototyping', 'Systems thinking'].map((x) => <span key={x} className="rounded-md border border-slate-700 bg-slate-900/60 px-2.5 py-1.5 text-xs text-slate-300">{x}</span>)}</div><button onClick={() => go('evidence')} className="mt-6 flex items-center gap-2 text-xs text-cyan-200" data-testid="button-view-evidence">Open evidence vault <ArrowRight size={13}/></button></div></div></div>; }

function Gaps({ go }: { go: (p: PageKey) => void }) { const gaps = [{ name: 'Product prioritization', score: 38, why: 'Turn customer evidence into a clear sequence of bets.', time: '2.5 hrs', color: 'purple' }, { name: 'Success metrics', score: 46, why: 'Connect a design decision to an outcome you can measure.', time: '1.5 hrs', color: 'cyan' }, { name: 'Case study narrative', score: 57, why: 'Make your decisions legible to someone who was not in the room.', time: '2 hrs', color: 'green' }, { name: 'Systems thinking', score: 63, why: 'Show how a feature lives within a larger product.', time: '3 hrs', color: 'amber' }]; return <div className="enter space-y-9"><SectionHead eyebrow="your gap map" title="Spend energy where it compounds." desc="These are demo recommendations based on the profile you started with. Edit your signals to make them yours." action={<Button variant="outline" onClick={() => {}} testId="button-refresh-gaps"><RefreshCw size={14}/> Re-analyze</Button>}/><div className="grid gap-4 lg:grid-cols-2">{gaps.map((g, i) => <button key={g.name} onClick={() => go('learn')} className="glass group rounded-2xl p-5 text-left transition-transform hover:-translate-y-0.5" data-testid={`card-gap-${i}`}><div className="flex items-start justify-between"><div className="flex items-center gap-3"><div className={`grid size-9 place-items-center rounded-lg ${g.color === 'purple' ? 'bg-purple-300/10 text-purple-200' : g.color === 'green' ? 'bg-emerald-300/10 text-emerald-200' : 'bg-cyan-300/10 text-cyan-200'}`}><Target size={17}/></div><div><h3 className="font-semibold text-white">{g.name}</h3><p className="mt-1 text-xs text-slate-500">{g.time} to first signal</p></div></div><span className="mono text-xs text-slate-500">{g.score}%</span></div><div className="mt-5 h-1.5 rounded-full bg-slate-800"><div className={`h-full rounded-full ${g.color === 'purple' ? 'bg-purple-300' : g.color === 'green' ? 'bg-emerald-300' : 'bg-cyan-300'}`} style={{ width: `${g.score}%` }}/></div><p className="mt-4 text-sm leading-6 text-slate-400">{g.why}</p><div className="mt-4 flex items-center gap-2 text-xs font-semibold text-cyan-200 opacity-70 group-hover:opacity-100">Open learning path <ArrowRight size={13}/></div></button>)}</div><div className="glass flex flex-wrap items-center justify-between gap-4 rounded-2xl p-6"><div><div className="mono text-[10px] uppercase tracking-widest text-slate-500">the useful question</div><p className="mt-2 text-sm text-slate-300">What would move your readiness by 5 points this week?</p></div><Button onClick={() => go('practice')} testId="button-find-action">Find an action <ArrowRight size={14}/></Button></div></div>; }

function Learn({ notify }: { notify: (s: string) => void }) { const [filter, setFilter] = useState('All'); const resources = [{ title: 'Make better product trade-offs', kind: 'Practice brief', time: '12 min', tag: 'Product sense' }, { title: 'The anatomy of a decision log', kind: 'Read', time: '8 min', tag: 'Communication' }, { title: 'Metrics that tell a product story', kind: 'Workshop', time: '25 min', tag: 'Analytics' }, { title: 'A case study is a guided tour', kind: 'Field note', time: '6 min', tag: 'Storytelling' }]; return <div className="enter space-y-9"><SectionHead eyebrow="your learning plan" title="A small plan with a point of view." desc="Four resources selected for your current gaps. Finish one, then make something with it." action={<div className="flex gap-2">{['All', 'Read', 'Practice'].map((x) => <button key={x} onClick={() => setFilter(x)} className={`rounded-lg px-3 py-2 text-xs ${filter === x ? 'bg-cyan-200 text-slate-950' : 'border border-slate-800 text-slate-400'}`} data-testid={`filter-${x.toLowerCase()}`}>{x}</button>)}</div>}/><div className="grid gap-3 md:grid-cols-2">{resources.filter((r) => filter === 'All' || r.kind === filter || (filter === 'Practice' && r.kind === 'Workshop')).map((r, i) => <div key={r.title} className="glass group rounded-2xl p-5"><div className="flex items-center justify-between"><span className="rounded-md border border-slate-700 px-2 py-1 text-[10px] text-slate-400">{r.kind}</span><span className="mono text-[10px] text-slate-600">{r.time}</span></div><h3 className="display mt-12 text-xl font-semibold text-white">{r.title}</h3><div className="mt-2 text-xs text-cyan-200">{r.tag}</div><Button variant="ghost" className="mt-6 -ml-2" onClick={() => notify(`Opened “${r.title}” in demo mode.`)} testId={`button-resource-${i}`}>Open resource <ArrowRight size={14}/></Button></div>)}</div><div className="rounded-2xl border border-cyan-200/15 bg-cyan-200/[.04] p-6"><div className="flex gap-4"><Lightbulb className="shrink-0 text-cyan-200" size={20}/><div><h3 className="font-semibold text-white">Make the learning visible.</h3><p className="mt-1 text-sm leading-6 text-slate-400">After each resource, add one sentence to your decision log. That sentence is already becoming interview material.</p></div></div></div></div>; }

function Practice({ checked, setChecked, notify }: { checked: string[]; setChecked: (x: string[]) => void; notify: (s: string) => void }) { const [started, setStarted] = useState(false); const items = [{ id: 'research', text: 'What customer evidence would you ask for first?' }, { id: 'tradeoff', text: 'Which constraint changes your recommendation?' }, { id: 'measure', text: 'How would you know your decision worked?' }]; return <div className="enter space-y-9"><SectionHead eyebrow="practice studio" title="Practice the moment, not the theory." desc="A low-stakes scenario to sharpen your product point of view." action={<span className="mono text-[10px] uppercase tracking-widest text-slate-500">demo scenario · 12 min</span>}/><div className="grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><div className="glass rounded-2xl p-6 sm:p-8"><div className="flex items-center gap-2 text-xs text-cyan-200"><Play size={14}/> SCENARIO 01</div><h2 className="display mt-6 text-2xl font-semibold text-white">The quiet feature request</h2><p className="mt-4 leading-7 text-slate-400">A small business customer asks for five new dashboard filters. Support says the request is urgent. Product data shows most customers never visit the dashboard after setup.</p><div className="my-7 h-px bg-white/[.08]"/><p className="text-sm font-semibold text-white">Before you choose a direction, what do you want to learn?</p><div className="mt-5 space-y-3">{items.map((item) => <button key={item.id} onClick={() => setChecked(checked.includes(item.id) ? checked.filter((x) => x !== item.id) : [...checked, item.id])} className={`flex w-full items-center gap-3 rounded-lg border p-3 text-left text-sm transition-colors ${checked.includes(item.id) ? 'border-cyan-200/30 bg-cyan-200/[.07] text-cyan-50' : 'border-slate-800 text-slate-400 hover:border-slate-700'}`} data-testid={`check-${item.id}`}><span className={`grid size-5 place-items-center rounded border ${checked.includes(item.id) ? 'border-cyan-200 bg-cyan-200 text-slate-950' : 'border-slate-700'}`}>{checked.includes(item.id) && <Check size={12}/>}</span>{item.text}</button>)}</div><Button onClick={() => { setStarted(true); notify('Reflection saved to your practice log.'); }} className="mt-7" testId="button-save-reflection">{started ? 'Reflection saved' : 'Save my reflection'} <Check size={14}/></Button></div><div className="rounded-2xl border border-purple-200/15 bg-[radial-gradient(circle_at_80%_15%,rgba(117,89,206,.18),transparent_45%),#111225] p-6 sm:p-8"><div className="mono text-[10px] uppercase tracking-[.22em] text-purple-200">coach note</div><div className="mt-10"><Mic2 className="text-purple-200" size={22}/><h3 className="display mt-5 text-xl font-semibold text-white">Lead with a question.</h3><p className="mt-3 text-sm leading-6 text-slate-400">Strong product thinkers resist the fastest answer. In your response, name the uncertainty you would reduce before spending build time.</p></div><div className="mt-12 border-t border-white/[.08] pt-5 text-xs text-slate-500">Your notes stay local to this demo workspace.</div></div></div></div>; }

function Evidence({ notify }: { notify: (s: string) => void }) { const [tab, setTab] = useState('Projects'); return <div className="enter space-y-9"><SectionHead eyebrow="evidence vault" title="Let the work speak." desc="Collect the artifacts that make your growth easy to see." action={<Button onClick={() => notify('Demo upload flow opened.')} testId="button-add-evidence"><Plus size={15}/> Add evidence</Button>}/><div className="flex gap-6 border-b border-white/[.08]">{['Projects', 'Assessments', 'Signals'].map((x) => <button key={x} onClick={() => setTab(x)} className={`border-b-2 pb-3 text-sm ${tab === x ? 'border-cyan-200 text-cyan-100' : 'border-transparent text-slate-500'}`} data-testid={`tab-${x.toLowerCase()}`}>{x}</button>)}</div>{tab === 'Projects' ? <div className="grid gap-4 md:grid-cols-2"><div className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><span className="rounded-md bg-emerald-300/10 px-2 py-1 text-[10px] text-emerald-200">ready to share</span><MoreHorizontal size={16} className="text-slate-600"/></div><h3 className="display mt-12 text-xl font-semibold text-white">Transit app redesign</h3><p className="mt-2 text-sm text-slate-500">Research · Flows · Usability testing</p><div className="mt-6 flex items-center gap-2 text-xs text-cyan-200"><FileText size={14}/> Case study outline complete</div></div><div className="rounded-2xl border border-dashed border-slate-700 p-6"><div className="grid size-9 place-items-center rounded-lg bg-slate-900 text-slate-500"><Plus size={17}/></div><h3 className="mt-12 font-semibold text-white">Add a project</h3><p className="mt-2 text-sm text-slate-500">Classwork, experiments, or work in progress all count.</p><Button variant="outline" className="mt-6" onClick={() => notify('Demo project form opened.')} testId="button-add-project">Add project</Button></div></div> : <div className="glass rounded-2xl p-8 text-center"><div className="mx-auto grid size-11 place-items-center rounded-full bg-slate-900 text-slate-500"><Search size={18}/></div><h3 className="mt-4 font-semibold text-white">Nothing here yet</h3><p className="mt-2 text-sm text-slate-500">Complete a practice session or add a signal to grow this layer.</p></div>}</div>; }

function Opportunities({ notify }: { notify: (s: string) => void }) { const [saved, setSaved] = useState<string[]>(['northstar']); const opps = [{ id: 'northstar', company: 'Northstar Labs', role: 'Associate Product Designer', match: '76%', note: 'Strong craft match · build metrics proof' }, { id: 'fieldnote', company: 'Fieldnote', role: 'Product Design Fellow', match: '68%', note: 'Great research overlap · portfolio depth needed' }, { id: 'commonroom', company: 'Common Room', role: 'Junior Product Designer', match: '61%', note: 'Systems thinking opportunity · early stage team' }]; return <div className="enter space-y-9"><SectionHead eyebrow="opportunity tracker" title="Keep the horizon in view." desc="Compare opportunities by the work they ask for, not just the name on the door." action={<Button onClick={() => notify('Demo opportunity form opened.')} testId="button-add-opportunity"><Plus size={15}/> Add opportunity</Button>}/><div className="space-y-3">{opps.map((o) => <div key={o.id} className="glass flex flex-wrap items-center gap-5 rounded-2xl p-5"><div className="grid size-11 place-items-center rounded-xl border border-slate-700 bg-slate-900 font-semibold text-cyan-200">{o.company.slice(0, 1)}</div><div className="min-w-[190px] flex-1"><h3 className="font-semibold text-white">{o.role}</h3><p className="mt-1 text-xs text-slate-500">{o.company} · {o.note}</p></div><div className="text-right"><div className="display text-2xl font-semibold text-white">{o.match}</div><div className="mono text-[9px] uppercase tracking-widest text-slate-600">fit signal</div></div><button onClick={() => setSaved(saved.includes(o.id) ? saved.filter((x) => x !== o.id) : [...saved, o.id])} className={`rounded-lg border px-3 py-2 text-xs ${saved.includes(o.id) ? 'border-cyan-200/30 bg-cyan-200/10 text-cyan-100' : 'border-slate-700 text-slate-500'}`} data-testid={`button-save-opportunity-${o.id}`}>{saved.includes(o.id) ? 'Saved' : 'Save'}</button></div>)}</div></div>; }

function Journey() { const milestones = ([['Orientation', 'You named a direction and chose one real opportunity.', true], ['First signal', 'Your first practice reflection is in the vault.', true], ['Proof loop', 'Make one small artifact that answers a real question.', false], ['Ready room', 'Tell the story with calm and specificity.', false]] as Array<[string, string, boolean]>); return <div className="enter space-y-9"><SectionHead eyebrow="progress journey" title="Notice the distance you have crossed." desc="Readiness is built in connected steps. This is your trail, not a leaderboard."/><div className="relative max-w-2xl pl-8">{milestones.map(([title, desc, done], i) => <div key={title} className="relative pb-12 last:pb-0"><div className={`absolute -left-8 top-0 grid size-6 place-items-center rounded-full border ${done ? 'border-cyan-200 bg-cyan-200 text-slate-950' : 'border-slate-700 bg-[#080a13] text-slate-600'}`}>{done ? <Check size={13}/> : <span className="text-[10px]">{i + 1}</span>}</div>{i < milestones.length - 1 && <div className={`absolute -left-[19px] top-6 h-full w-px ${done ? 'bg-cyan-200/40' : 'bg-slate-800'}`}/>}<div className="glass rounded-xl p-5"><div className="mono text-[9px] uppercase tracking-widest text-slate-600">chapter {String(i + 1).padStart(2, '0')}</div><h3 className="mt-2 font-semibold text-white">{title}</h3><p className="mt-2 text-sm text-slate-500">{desc}</p></div></div>)}</div></div>; }

function Reference() { const cards = [['Interview language', 'Use “I noticed…”, “I tested…”, and “I changed…” to keep your story specific.'], ['Evidence check', 'Can someone else see the decision, the constraint, and the result?'], ['Good next action', 'Small enough to finish. Connected to a gap. Visible when complete.'], ['When stuck', 'Name the uncertainty before searching for the answer.']]; return <div className="enter space-y-9"><SectionHead eyebrow="quick reference" title="Keep the signal close." desc="A few reminders for the moments when the path gets noisy."/><div className="grid gap-3 sm:grid-cols-2">{cards.map(([title, text], i) => <div className="glass rounded-2xl p-6" key={title}><div className="mono text-[10px] text-cyan-200">0{i + 1}</div><h3 className="mt-10 font-semibold text-white">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-400">{text}</p></div>)}</div></div>; }

function SettingsPanel({ notify }: { notify: (s: string) => void }) { return <div className="enter space-y-9"><SectionHead eyebrow="workspace settings" title="Make the space fit you." desc="These controls are local to the demo experience."/><div className="glass max-w-2xl divide-y divide-white/[.07] rounded-2xl"><div className="flex items-center justify-between gap-4 p-6"><div><h3 className="font-semibold text-white">Learning reminders</h3><p className="mt-1 text-sm text-slate-500">A gentle nudge when your next action is waiting.</p></div><button onClick={() => notify('Learning reminders toggled.')} className="h-6 w-11 rounded-full bg-cyan-300 p-1" data-testid="toggle-reminders"><span className="ml-5 block size-4 rounded-full bg-slate-950"/></button></div><div className="flex items-center justify-between gap-4 p-6"><div><h3 className="font-semibold text-white">Demo data</h3><p className="mt-1 text-sm text-slate-500">No external accounts are connected in this preview.</p></div><span className="rounded-md border border-emerald-300/20 bg-emerald-300/10 px-2 py-1 text-[10px] text-emerald-200">local only</span></div><div className="p-6"><Button variant="outline" onClick={() => notify('Profile editor is ready.')} testId="button-edit-settings">Edit profile details</Button></div></div></div>; }

function Router() { const [mode, setMode] = useState<'landing' | 'onboard' | 'app'>('landing'); return <Switch><Route path="/"><>{mode === 'landing' && <Landing enter={() => setMode('onboard')}/>} {mode === 'onboard' && <Onboarding finish={() => setMode('app')} back={() => setMode('landing')}/>} {mode === 'app' && <AppShell exit={() => setMode('landing')}/>}</></Route><Route><Landing enter={() => setMode('onboard')}/></Route></Switch>; }
function RoutedErrorBoundary({ children }: { children: ReactNode }) { const [location] = useLocation(); return <ErrorBoundary resetKey={location}>{children}</ErrorBoundary>; }
function App() { return <QueryClientProvider client={queryClient}><TooltipProvider><WouterRouter base={import.meta.env.BASE_URL.replace(/\/$/, '')}><RoutedErrorBoundary><Router/></RoutedErrorBoundary></WouterRouter><Toaster/></TooltipProvider></QueryClientProvider>; }
export default App;